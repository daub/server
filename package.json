{
  "name": "@daub/server",
  "version": "0.0.1",
  "scripts": {
    "test": "lerna bootstrap && lerna run test",
    "lint": "lerna run lint",
    "dev": "nodemon server.js"
  },
  "devDependencies": {
    "lerna": "^2.1.0",
    "nodemon": "^1.12.0"
  },
  "dependencies": {
    "config": "^1.26.2"
  }
}
