{
  "name": "@daub/test-middleware",
  "version": "0.0.1",
  "main": "lib/index.js",
  "keywords": [
    "test",
    "server",
    "middleware",
    "axios"
  ],
  "scripts": {
    "test": "ava -s --fail-fast",
    "lint": "standard --fix",
    "qualify": "npm test && npm run lint",
    "prepublishOnly": "npm run qualify"
  },
  "license": "UNLICENSED",
  "devDependencies": {
    "ava": "^0.22.0",
    "standard": "^10.0.3"
  },
  "dependencies": {
    "@daub/server": "^0.0.1",
    "axios-serve": "^1.1.0",
    "mongomem": "^0.1.9"
  },
  "ava": {
    "files": [
      "test/**/*.spec.js"
    ]
  }
}
