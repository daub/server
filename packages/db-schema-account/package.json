{
  "name": "@daub/db-schema-account",
  "version": "0.0.1",
  "main": "lib/index.js",
  "keywords": [
    "db",
    "schema",
    "user",
    "account",
    "auth"
  ],
  "scripts": {
    "test": "ava -s --fail-fast",
    "lint": "standard --fix",
    "qualify": "npm test && npm run lint",
    "prepublishOnly": "npm run qualify"
  },
  "license": "UNLICENSED",
  "devDependencies": {
    "ava": "^0.22.0",
    "@daub/test-mongoose": "^0.0.1",
    "@daub/db-schema-password": "^0.0.1",
    "standard": "^10.0.3"
  },
  "dependencies": {
    "@daub/db-schema": "^0.0.1",
    "bcrypt": "^1.0.3"
  },
  "ava": {
    "files": [
      "test/**/*.spec.js"
    ]
  }
}
