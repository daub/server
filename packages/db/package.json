{
  "name": "@daub/db",
  "version": "0.0.1",
  "main": "lib/index.js",
  "keywords": [
    "db"
  ],
  "scripts": {
    "test": "ava -s --fail-fast",
    "lint": "standard --fix",
    "qualify": "npm test && npm run lint",
    "prepublishOnly": "npm run qualify"
  },
  "license": "UNLICENSED",
  "devDependencies": {
    "ava": "^0.22.0",
    "mongomem": "^0.1.9",
    "standard": "^10.0.3"
  },
  "dependencies": {
    "@daub/db-schema": "^0.0.1",
    "@daub/db-schema-thing": "^0.0.1",
    "@daub/db-schema-account": "^0.0.1",
    "mongoose": "^4.11.10"
  },
  "ava": {
    "files": [
      "test/**/*.spec.js"
    ]
  }
}
